
import * as React from 'react'
import { View, ViewProps, ViewStyle ,Text,TextProps} from 'react-primitives'

interface IFontAwesomeProps { }
interface IFontAwesomeState { }

export class FontAwesome extends React.Component<IFontAwesomeProps, IFontAwesomeState> {
    render() {
        return <Text>{name + 'wh'}</Text>
    }
}
