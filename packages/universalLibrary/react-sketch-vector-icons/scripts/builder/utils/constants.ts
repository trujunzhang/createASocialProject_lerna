import { icons } from '../../icons'
import path from 'path'

// file path
const rootDir = path.resolve(__dirname, '../../../')
const DIST = path.resolve(rootDir, './src/svgs')

const ICONS = 'icons'
export { icons, rootDir, DIST, ICONS }
