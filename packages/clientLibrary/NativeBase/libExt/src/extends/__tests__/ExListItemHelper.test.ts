// import { getExListItemState } from '../ExListItemHelper'
// import { IExListItemProps } from '../../../@types/extends'

describe('functions of the ExListItemHelper', () => {
  it('should return correctly, invoke getExListItemState, only centerElement', () => {
    // const props: IExListItemProps = {
    //   centerElement: 'my primary text'
    // }
    // const result = getExListItemState(props)
    // expect(JSON.stringify(result)).toBe(
    //   JSON.stringify({
    //     hasLeft: false,
    //     oneLineCenter: true
    //   })
    // )
  })

  it('should return correctly, invoke getExListItemState, have leftElement', () => {
    // const props: IExListItemProps = {
    //   leftElement: 'arrow-back',
    //   centerElement: 'my primary text'
    // }
    // const result = getExListItemState(props)
    // expect(JSON.stringify(result)).toBe(
    //   JSON.stringify({
    //     hasLeft: true,
    //     oneLineCenter: true
    //   })
    // )
  })
})
