

import { platformVariables as variables } from "./../variables/platform";

export default (variables /*: * */ = variable) => {
  const iconTheme = {
    fontSize: variables.iconFontSize,
    color: "#000",
      getIcon:(iconType)=>{return 'djzhangxxx'}
  };

  return iconTheme;
};
