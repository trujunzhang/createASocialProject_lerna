import { themeVariables } from '@app/native-base-variables'
export const iconTheme = (variables: themeVariables) => {
  const iconTheme = {
    fontSize: variables.iconFontSize,
    color: '#000'
  }

  return iconTheme
}
