import { themeVariables } from '@app/native-base-variables'

export function getVerticalItem(params, variables: themeVariables, platform, platformStyle) {
  return {
    flexDirection: 'column',
    height: null
  }
}
