import { themeVariables } from '@app/native-base-variables'

export const leftTheme = (variables: themeVariables) => {
  const leftTheme = {
    flex: 1,
    alignSelf: 'center',
    alignItems: 'flex-start'
  }

  return leftTheme
}
