import { themeVariables } from '@app/native-base-variables'

export const bodyTheme = (variables: themeVariables) => {
  const bodyTheme = {
    flex: 1,
    alignItems: 'center',
    alignSelf: 'center'
  }

  return bodyTheme
}
