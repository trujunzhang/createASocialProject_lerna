import { themeVariables } from '@app/native-base-variables'

export function getBlockItem(params, variables: themeVariables, platform, platformStyle) {
  return {
    justifyContent: 'center',
    alignSelf: 'stretch'
  }
}
