import { themeVariables } from '@app/native-base-variables'
export const tabTheme = (variables: themeVariables) => {
  const tabTheme = {
    flex: 1,
    backgroundColor: '#FFF'
  }

  return tabTheme
}
