import { themeVariables } from '@app/native-base-variables'

export const labelTheme = (variables: themeVariables) => {
  const labelTheme = {
    '.focused': {
      width: 0
    },
    fontSize: 17
  }

  return labelTheme
}
