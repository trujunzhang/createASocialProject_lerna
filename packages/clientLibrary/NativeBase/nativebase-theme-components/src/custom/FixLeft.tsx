import { themeVariables } from '@app/native-base-variables'
export const fixLeftTheme = (variables: themeVariables) => {
  const fixLeftTheme = {
    alignSelf: 'center',
    alignItems: 'flex-start'
  }

  return fixLeftTheme
}
