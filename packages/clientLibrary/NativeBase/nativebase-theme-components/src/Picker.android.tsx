import { themeVariables } from '@app/native-base-variables'
export const pickerTheme = (variables: themeVariables) => {
  const pickerTheme = {
    '.note': {
      color: '#8F8E95'
    },
    // width: 90,
    marginRight: -4,
    flexGrow: 1
  }

  return pickerTheme
}
