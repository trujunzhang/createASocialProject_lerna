import { IBaseVariables } from '@app/tools' // typings

import { platform } from '../../utils'

const platformStyle = 'material'
export const baseVariables: IBaseVariables = {
  platformStyle,
  platform
}
