import { IThemeVariables } from '@app/tools' // typings

// import * as Props from './props'
// import * as Base from './base'

export const spinnerTheme = (variables: IThemeVariables) => {
  const spinnerTheme = {
    height: 80
  }

  return spinnerTheme
}
