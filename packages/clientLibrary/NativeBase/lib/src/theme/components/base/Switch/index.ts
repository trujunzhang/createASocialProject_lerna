import { IThemeVariables } from '@app/tools' // typings

// import * as Props from './props'
// import * as Base from './base'

export const switchTheme = (variables: IThemeVariables) => {
  const switchTheme = {
    marginVertical: -5
  }

  return switchTheme
}
