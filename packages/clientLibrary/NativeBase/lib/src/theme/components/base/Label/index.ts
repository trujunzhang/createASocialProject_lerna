import { IThemeVariables } from '@app/tools' // typings

// import * as Props from './props'
// import * as Base from './base'

export const labelTheme = (variables: IThemeVariables) => {
  const labelTheme = {
    '.focused': {
      width: 0
    },
    fontSize: 17
  }

  return labelTheme
}
