import { IThemeVariables } from '@app/tools' // typings

// import * as Props from './props'
// import * as Base from './base'

export const bodyTheme = (variables: IThemeVariables) => {
  const bodyTheme = {
    flex: 1,
    alignItems: 'center',
    alignSelf: 'center'
  }

  return bodyTheme
}
