import { IThemeVariables } from '@app/tools' // typings

// import * as Props from './props'
// import * as Base from './base'

export const tabTheme = (variables: IThemeVariables) => {
  const tabTheme = {
    flex: 1,
    backgroundColor: '#FFF'
  }

  return tabTheme
}
